# 面试准备手册：Memory+Planning Agent & 智舱指令 Agent

> 本手册用于你面试前的系统化梳理，核心思路：**把做过的事讲成"我发现了什么问题 → 我为什么这么取舍 → 结果/改进"**，而不是背名词。

---

## 一、30 秒开场介绍稿

> 我做了一个"从原理到工程化"的大模型 Agent 项目，主线是：**先自己从零造一个 ReAct Agent，再用 LangGraph 做工程化优化**。分两条线——第一块是**记忆 + 自主规划**：分级记忆（短期 + 长期）、冲突去重、LRU 遗忘，规划→执行→反思的闭环；第二块是**智舱指令 Agent**：把通用 Agent 改造成车载环境里"带安全门"的受控指令执行器。两者的共同点是**记忆贯穿始终**，以及**在真实工程里反复暴露和解决过具体问题**（比如 SQLite 线程安全、安全门被模型绕过、偏好记忆去重策略选错，和最近这次 running-event-loop 踩坑）。

---

## 二、系统详解（分模块：是什么 / 怎么实现 / 为什么这么设计 / 可讲的点）

### 1. 记忆系统（`memory/`）

- **设计**：短期（进程内存、有界）+ 长期（真实 Embedding 向量存 SQLite + 结构化元数据）。`MemoryManager` 统一召回/写入。
- **写入时机**：[manager.py](memory/manager.py) 里对"目标/事实/教训/偏好/目的地"升级长期，普通对话留短期。
- **召回**：`recall()` 合并长期语义检索命中 + 短期最近上下文，注入 System Prompt。
- **冲突去重**：写入长期前用 Embedding 找最相似记忆，相似度 ≥0.85 合并。
- **遗忘**：超 `memory_capacity`(200) 后按最近访问时间删除最久未用（LRU）。

> **面试可讲的点（最重要）**：**去重策略必须按记忆类型选**。
> - 我从真实 bug 发现：偏好是**单值**（一个温度），用"相似度合并"会造成 `24℃；24℃` 无限累加；换值还会 `24℃；20℃` 累积。
> - 正确做法：事实/经验用相似度合并（累积增量有意义），**偏好/目的地用"同一主题 latest-wins"覆盖**。
> - 一句话总结：**"去重不是无脑相似度合并，而是看这条记忆是"越用越丰富"还是"最新覆盖旧"。"**

---

### 2. 规划闭环（`planner/`）

- **流程**：Planner 把目标拆成任务树 → Executor 逐个执行叶子任务 → Reflector 抽出 lesson/fact 写回长期记忆 → 下一次决策可召回复用。
- **记忆续接**：Planner 也会按目标召回记忆注入上下文，所以用户第二次含糊地"再规划一下"，依然能定位到"西安"。

> **可讲的点**：规划不是孤立模块，它和记忆、工具、LLM 是闭环的；尤其"规划的执行任务"和"规划的拆解"都必须有记忆可见性，否则多轮就"失忆"。

---

### 3. ReAct 主循环 + 工具（`core/agent.py`, `tools/`）

- 解析模型返回的 `tool_calls` → 在 `ToolRegistry` 里执行 → 回填 `ToolMessage` → 再给模型，直到无工具调用的最终回答（`max_agent_steps` 控上限）。
- 支持**单次响应的多条工具调用**（多 tool_calls 逐条执行）。
- 支持**流式**：`stream_turn` 逐 token 产出文本，同时捕获 tool_calls，工具执行时后台调用并回填。

> **可讲的点**：从自由文本动作名 → 收敛成 action 白名单 enum 的演进（见智舱），说明你理解"结构约束比依赖模型自觉可靠"。

---

### 3.5 编排演进：自研 ReAct → LangGraph（新增面试主线核心，重点讲）

**为什么从自研改成上 LangGraph**（回答"为什么要引框架"）：
- 自研 ReAct 先把每环讲透（状态、tool_calls 解析、记忆、规划、安全门），但**框架帮我省掉的都是样板代码**：`bind_tools` 的消息契约、`tool_calls → ToolMessage` 回填、以及最关键的中断-恢复 (`interrupt()` + Checkpointer)。
- 自研版做过一次受控二次确认：待确认队列 + `do_confirm` 按钮手动重放；LangGraph 版的 `ask_confirm` 节点直接 `interrupt()` 挂起，UI 确认时用 `Command(resume=...)` 从断点恢复——**同一安全门逻辑零改动**，只换了编排壳。

**两版对比（背这张表能体现工程判断力）**：

| 维度 | 自研 ReAct | LangGraph 优化 |
|---|---|---|
| 主循环 | 手写 `while` + 状态拼接 | `StateGraph` 图式状态机 + 条件边 |
| 工具调用回填 | 手写解析 `tool_calls` | `bind_tools` + langchain 消息契约 |
| 同句多条指令 | for 循环串行处理 | 图节点内循环，行为一致 |
| 受控指令二次确认 | 待确认队列 + 手动重放 | `interrupt()` + `MemorySaver` Checkpointer 恢复 |
| 断点/续跑 | 无 | 支持中断-恢复、时间旅行 |
| 依赖 | 无第三方（自研 LLM SDK） | 需 `langgraph` + `langchain-openai` |

> **可讲的点（三句话版本）**：
> 1. "先自研是为了搞懂原理，再上框架是为了工程化"——业务逻辑我只写一遍、复用，所以对比两版成本极低。
> 2. 引框架要**克制**：只装 `langgraph` + `langchain-openai` 两个包，没引入 SDK/检查点持久化，interrupt 用进程内 MemorySaver 就够演示。
> 3. 面试加分实锤：我因为混用同步图节点 + `asyncio.run()` 踩到 running-event-loop 的坑（Streamlit uvloop 下 `_run` 报错），改成"新线程跑干净 loop"才通过——这是真实工程里 async/sync 边界问题的活例。

---

### 4. 智舱指令层（`cockpit/`）

数据流：`raw_input → 意图理解(LLM function calling) → VehicleCommand(结构化 + Action 白名单) → SafetyGate → 执行/确认/拦截/问答`。

- [schemas.py](cockpit/schemas.py)：`VehicleCommand`（域/动作/参数/目标/优先级）+ `SafetyLevel` 四级 + `Action` 白名单。
- [safety.py](cockpit/safety.py)：按"域+动作"查表分级 + 危险词正则兜底。
- [state.py](cockpit/state.py)：模拟 `VehicleState`，独立于业务可整体替换为真实 CAN 实现。
- [controller.py](cockpit/controller.py)：编排意图/记忆/安全门/执行，含 pending 二次确认队列。

**安全门四级分流**：

| 级别 | 例子 | 行为 |
|---|---|---|
| 🔵 INFO | "现在车速多少？" | 直接问答 |
| 🟢 COMFORT | "空调 24 度 / 音量 15" | 直接执行 |
| 🟡 CONTROLLED | "自动泊车 / 定速巡航" | **二次确认后执行** |
| 🔴 FORBIDDEN | "关掉 ESP / 超速" | 拦截 |

> **可讲的点（重点，三件事）：**
> 1. **安全不能依赖 LLM 自觉**：真实发现模型能把"把 ESP 关掉"解析成无害的 `disable` 动作、绕成舒适级。修法是把动作限定成 `Action` 白名单 `enum`，`safety` 域一刀切拦截。
> 2. **危险语义优先于结构化判定**：即使模型把危险输入错解析成无害动作，危险词规则仍会拦截（双保险）。
> 3. **受控指令的二次确认是"精确暂存 + 重放"**：需要确认的指令被原样存入 pending 队列，用户确认后从队列重放执行，而不是把句子重新丢给模型猜——这样参数永远精确。

---

### 5. 工程细节（真实踩坑 + 解法，面试超加分的素材）

| 问题 | 表现 | 解法 | 能体现的能力 |
|---|---|---|---|
| 导入异常 | `cannot import name 'FileResponse' from 'fastapi'` | 改从 `fastapi.responses` 导入 | 知道 FastAPI 与 Starlette 的包结构 |
| 循环导入 | core/__init__ 急切导入导致循环 | `core/__init__.py` 改惰性 `__getattr__` | 模块设计 / 惰性导入 |
| SQLite 线程安全 | Streamlit 多线程 rerun 触发 `ProgrammingError` | `check_same_thread=False` + `threading.Lock`，锁内不含 await | 并发与 asyncio 理解 |
| 多指令只执行第一条 | 一次输入多次 tool_call，只处理 `[0]` | 遍历所有 tool_calls 逐条执行 + `results` 落 UI | 鲁棒性 |
| 参数键不匹配 | 「风量2档」回成「3档」 | 执行器兼容 `fan_level`/`level` | 规范与兼容 |
| 偏好记忆累积 | `24℃；24℃` | 偏好改 latest-wins（上面已讲） | 领域化设计 |
| async/sync 混用 | LangGraph 同步图节点里调 `asyncio.run()` 报 `running event loop` | 记忆写改放**新线程跑干净 loop** | 对 LangGraph 执行模型 / asyncio 边界的理解 |
| Streamlit 热载不同步 | UI 新、逻辑旧，报 AttributeError | 改被 import 的模块需**重启进程** | 对框架运行时机制的理解 |
| 强调"调试不是改完就跑，而是先看它为什么错" | — | 每个问题都定位根因 | 工程素养 |

---

## 三、推荐的演示流程（约 18 分钟，含话术）

**开场（2 分钟）**：30 秒介绍稿讲完，先给主线"**先自研搞懂原理，再用 LangGraph 做工程化**"，再说"我给您演一遍这两套引擎"。

**记忆 + 规划（5 分钟）**
1. "帮我规划一趟去西安的 3 日旅游" → 展示任务树拆解与执行。
2. "再规划一下" → **强调**：不问目的地也续上了西安——这是记忆续接。
3. "记住我空调要 20 度" → 长期记忆面板出现偏好。

**智舱 + 安全（5 分钟）**
1. "空调调到 24 度，音量 15" → 舒适级直接执行，车况面板更新。
2. "自动停个车" → 🟡 受控，弹出待确认面板 → 点确认 → 执行。
3. "把 ESP 关掉" → 🔴 拦截，明确系统拦住了危险操作。

**编排对比（3 分钟，本项目的差异化亮点）**
1. 侧边栏切到 LangGraph → 重发"空调调到 24 度" → **强调**：同一记忆池、同一安全门，只是外层的图（`StateGraph`）换了。
2. 发"自动停个车"（LangGraph 版）→ 走 `interrupt()` 挂起 → 点确认从断点恢复执行。
3. 一句话收口："业务逻辑我只写了一遍，两版都能跑，这就是'先自研、再上框架'的价值。"

**收尾（3 分钟）**：主动讲一个"我踩过的 bug 和我怎么改的"——三选一：SQLite 线程安全、偏好 latest-wins、或 LangGraph running-event-loop（同步节点里 `asyncio.run` 报错 → 新线程跑干净 loop）。推荐讲 running-event-loop 这个最新的，能体现你对 async/sync 边界的理解。

---

## 四、面试注意

**DO**
- 永远用"问题 → 取舍 → 结果"句式，别背 API。
- 主动暴露边界并给出"如果是生产环境我会怎么做"：例如 `VehicleState` 模拟 → "接桥接层换真实 CAN 即可"；Embedding 降级 → "离线可用是特性"。
- 把话引到你的强项：记忆策略、安全门设计。
- 诚实：说错就纠正，被问到不会的直接说"这块我做的是 X，深入的是我熟悉的 Y"。

**DON'T**
- 不用"高大上、业界领先"这类词（会被认为在注水）。
- 不要只强调"我加了记忆"而不解释**为什么这么设计**。
- 不要在安全话题上嘴软——这正是你这个项目区别于一般 chatbot 的地方，要大大方方讲。

---

## 五、高频问题 Q&A（含参考答案）

**Q1：你自研了 ReAct，为什么后来又引入 LangGraph？**
> 逻辑是"**先搞懂原理，再做工程化**"：自研 ReAct 只有几百行，让我把每个环节（状态拼接、`tool_calls` 解析、记忆、规划、安全门）讲透，这是地基；但框架确实省大量样板——最值的是 `bind_tools` 消息契约、`ToolMessage` 回填，以及智舱那套 `interrupt()` + Checkpointer 的中断-恢复。关键是**两版只写一遍业务、互相复用**，切换对比近乎零成本，所以不是打脸，而是同一份业务的两套表达。引框架我也克制，只装 `langgraph` + `langchain-openai`。一句话：**不引框架你不懂原理，只引框架你不懂取舍**。

**Q2：记忆是怎么存的和检索的？**
> 长期记忆用真实 Embedding（OpenAI 兼容 `/embeddings`）计算向量，连同结构化元数据一起存 SQLite；检索用余弦相似度召回 Top-K，再合并短期最近上下文注入 prompt。没配 Embedding 端点时降级成哈希向量，保证离线可跑。

**Q3：为什么 SQLite 会线程崩，你怎么解决的？**
> SQLite 连接默认绑定创建线程。Streamlit 每次 rerun 可能换线程访问记忆，报 `ProgrammingError`。我设 `check_same_thread=False` 让它跨线程复用，并用一把锁把所有 SQLite 读写串行化；关键点是锁内只做同步语句、不 `await`，避免单线程事件循环死锁。

**Q4：多条指令怎么处理？如何保证安全不被绕过？**
> 模型可以在一次响应里返回多个 `tool_calls`，我逐条构造 `VehicleCommand`、逐条过安全门、逐条执行，结果都进 UI。安全上：动作收敛成 `Action` 白名单 enum（防止模型造出"看似无害的动作名绕过分级"），且危险词规则在结构化判定之外再兜底一层。

**Q5：安全门的"二次确认"具体怎么实现？**
> 受控指令过门后不直接执行，而是**原样存入 pending 队列**（含完整参数）。用户点"确认"后 `confirm_pending()` 从队列重放执行——不重新让模型生成，所以参数不会丢、也不会答非所问。

**Q6：偏好和事实的去重为什么不同？**
> 事实是"越用越丰富"，相似度高就该合并累积；偏好是单值（当前喜欢多少度），合并会造成 `24℃；24℃` 累加污染。所以偏好/目的地用"同一主题 latest-wins"，写前先清同主题旧条目，只留最新值。

**Q7：规划 How do you 防止"第二次就失忆"？**
> 规划拆解时也会先召回记忆注入上下文。第一次把目标（西安）沉淀成可召回的长期记忆，第二次"再规划一下"就能召回续接——这正是记忆和规划闭环的价值。

**Q8：生产上这套怎么落地？**
> 记忆库可换向量数据库（pgvector/Chroma），`VehicleState` 换真实 ECU/CAN 桥接，LLM 网关化并做多级缓存与降级；同样的安全门 + 白名单逻辑可直接复用。演示项目聚焦"设计与取舍"，生产化主要是换依赖、加可观测与评测。

**Q9：你怎么评测它做得好不好 / 有没有评测？**
> 目前偏演示，核心是**闭环链路能真实跑通**（记忆、规划、安全门的交叉用例在我的回归验证里都过了）。要做到"可量化"，我会加一个评测脚本：同一问题分别"带记忆 vs 不带记忆""带安全门 vs 不带"跑对比，统计召回命中率、指令正确率、危险拦截率。这是一个诚实且可执行的下一步。

**Q10：LangGraph 版受控指令的"二次确认"怎么断点恢复？**
> 图里加了一个 `ask_confirm` 同步节点，执行到 `interrupt({...})` 便挂起，配合 `MemorySaver` checkpointer 把该 thread 的完整状态存下来。UI 点"确认"时用 `Command(resume={"confirmed": True})` 以同一个 thread 恢复，图从 `ask_confirm` 下游继续，`execute_controlled` 直接执行暂存的那批指令。因为走的是 **checkpointer 恢复而非重新让模型生成**，所以挂起期间的参数一条不丢。

**Q11：自研和 LangGraph 两版会不会行为不一致、切换就"失忆"？**
> 不会。我做三件事兜底：一是**接口对齐**——Agent 都暴露 `run/run_stream`，Cockpit 都暴露 `handle/confirm_pending/has_pending`，UI 只换对象；二是**状态共享**——两版持有同一个 `MemoryManager` 和同一个 `VehicleState`，切引擎不会丢记忆和车况；三是**回归兜底**——`scripts/test_langgraph.py` 一次跑通 Agent + Cockpit + `interrupt` 确认链路。剩的行为差异基本只有 LLM 自身波动。

---

## 六、诚实边界清单（主动坦白，反而加分）

1. **智舱是模拟车辆状态**：`VehicleState` 是内存模拟，接真实 CAN 只需替换该实现。
2. **Embedding 可降级为哈希向量**：没配端点时语义检索精度下降，但系统仍能跑——这是"离线可用"特性而非缺陷。
3. **记忆升长期靠规则/标注**：目前"是否重要"主要由调用方打标，未做自动"重要度评估器"（如 TCEN）。
4. **规划"修订"还是占位**：执行中途根据结果动态改任务树尚未做实，是你下一步能讲的增长点。
5. **无系统化评测**：见 Q9，这是明确的 TODO。
6. **LangGraph 的 interrupt 走进程内 MemorySaver**：中断-恢复在同进程内有效，**跨重启不持久**；投产可换 SQLite/redis Checkpointer（我刻意最小化，没引入 checkpoint 持久化依赖）。
7. **LangGraph 是可选路径**：未配置 LLM 端点时 LangGraph 路径会直接报错，自研 ReAct 降级仍能"能跑"——所以编排默认自研最稳。

---

*祝你面试顺利。核心记住一句话：这个项目最大的差异化不是功能多，而是你**能针对每个真实问题讲出取舍**，尤其是"自研 ReAct → LangGraph 优化"这条**演进线**——先有原理地基，再做工程化，业务只写一遍、双引擎可切换。把这条主线讲透，比背任何 API 都加分。*